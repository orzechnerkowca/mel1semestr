#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <math.h>
#include "winbgi2.h"


//void prostokat(int x, int y, int a,int b){								//Rysuje prostokąt o bokach a,b i środku w(x, y).
//	line(x - b / 2, y - a / 2, x + b / 2, y - a / 2);
//	line(x - b / 2, y - a / 2, x - b / 2, y + a / 2);
//	line(x + b / 2, y - a / 2, x + b / 2, y + a / 2);
//	line(x + b / 2, y + a / 2, x - b / 2, y + a / 2);
//}

//void kwadrat(int x, int y, int r) {										//Rysuje kwadrat o boku 2r, środku w (x,y) i wpisane koło o promieniu r.
//	circle(x, y, r);
//	line(x - r, y - r, x + r, y - r);
//	line(x - r, y - r, x - r, y + r);
//	line(x + r, y - r, x + r, y + r);
//	line(x + r, y + r, x - r, y + r);
//}

void main() {
	graphics(500, 500);
	//prostokat(250,250,50,100);
	//kwadrat(250, 250, 50);
	
	/*double a;																//wykres a^2
	a = -3.14;  
	while (a < 3.14) {
		circle(a*100 + 200, (- a * a)*100 + 100, 3);
		a = a + 0.01;
	}     */

	/*double a;																//Narysuj punkty o współrzędnych x = 100*sin(a) + 100 oraz y = 100⋅cos(a) + 100.
	a = 0;
	while (a < 500) {
		circle(100*sin(a) + 100, 100*cos(a) + 100, 1);
		a = a + 0.01;
	}   */

	/*double a;																//Narysuj punkty o współrzędnych x = 100 * sin(a) * cos(4*a) + 1000 oraz y=100 * cos(a) * cos4a + 100.
	a = 0;
	while (a < 100) {
		circle(100 * sin(a) * cos(4*a) + 1000, 100 * cos(a) * cos(4*a) + 100, 1);
		a = a + 0.01;
	}   */

	/*double a, r;															//Narysuj punkty o współrzędnych x=100*r*sin(a)+100 oraz y=100*r*cos(a)+100, gdzie r=(cosa+2)/3 (niech r będzie kolejną zmienną).
	a = 0;
	r = (cos(a) + 2) / 3;
	while (a < 100) {
		circle(100 * r*sin(a) + 100, 100 * r * cos(a) + 100, 1);
		a = a + 0.01;
	}	*/

wait();
}
